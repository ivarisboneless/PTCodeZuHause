function lightDisconnectEV3(brickObj)
% lightDisconnectEV3(brickObj) i think also the command disconnect would wor here ;) 

    % In MATLAB, removing the object is enough to close the connection.
    clear brickObj
end
